#version 330 core

out vec4 FragColor;

in vec3 FragPos;
in vec3 Normal;
in vec2 TexCoord;

uniform vec3 objectColor;
uniform sampler2D textureSampler;

void main()
{
    FragColor = vec4(objectColor, 1.0) * texture(textureSampler, TexCoord);
}
